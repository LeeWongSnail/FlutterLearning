import 'package:flutter/material.dart';

class AirPlayPage extends StatelessWidget {
  const AirPlayPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AirPlay'),
      ),
      body: Center(
        child: Text('AirPlayPage'),
      ),
    );
  }
}
